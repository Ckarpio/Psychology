/*
 * =====================================================================
 * routes/index.js — ГЛАВНЫЙ ФАЙЛ МАРШРУТОВ (роутер-агрегатор)
 * =====================================================================
 *
 * Этот файл собирает все маршруты приложения в одном месте.
 *
 * Что такое маршрут (route)?
 *   Маршрут — это правило: "когда приходит запрос на такой-то URL,
 *   выполни такую-то функцию".
 *
 *   Аналогия: представь ресепшн в больнице. Пациент приходит и говорит
 *   "мне к терапевту" — его направляют в кабинет 1.
 *   "Мне к хирургу" — направляют в кабинет 2.
 *   Этот файл — и есть такой "ресепшн" для URL-запросов.
 *
 * Как это работает вместе с app.js:
 *   В app.js написано:   app.use('/api', routes)
 *   В этом файле:        router.use('/emotions', ...)
 *
 *   Итого: GET /api/emotions → попадёт в emotions.routes.js
 *          GET /api/recommendation → попадёт в recommendation.routes.js
 *
 *   Путь складывается: /api (из app.js) + /emotions (из этого файла)
 * =====================================================================
 */

// ─── Шаг 1: Создаём роутер ─────────────────────────────────────────
// require('express').Router() — создаёт новый объект Router.
// Router — это мини-приложение, которое умеет обрабатывать маршруты.
// Мы можем добавлять в него маршруты, а потом подключить весь роутер
// к основному приложению через app.use().
const router = require('express').Router();

// ─── Шаг 2: Подключаем маршруты для эмоций ─────────────────────────
// router.use('/emotions', ...) — все запросы, начинающиеся с /emotions,
// будут обработаны маршрутами из файла emotions.routes.js
//
// require('./emotions.routes') — загружает роутер из файла emotions.routes.js
router.use('/emotions', require('./emotions.routes'));

// ─── Шаг 3: Подключаем маршруты для рекомендаций ────────────────────
// Аналогично: все запросы с /recommendation обработает recommendation.routes.js
router.use('/recommendation', require('./recommendation.routes'));

// ─── Шаг 4: Экспортируем роутер ─────────────────────────────────────
// Этот роутер подключается в app.js через: app.use('/api', routes)
module.exports = router;
